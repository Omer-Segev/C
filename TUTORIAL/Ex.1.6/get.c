#include <stdio.h>

/* copy input to output */

main(){

/*
	int c; 
	while(c = (getchar() != EOF)){
		printf("%d\n", c);
	}
	printf("at EOF c is %d\n", c);
*/
	printf("The value of EOF is %d\n", EOF);
}

